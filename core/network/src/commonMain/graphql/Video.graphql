query Video($id: ID) {
    video(id: $id) {
        animatedPreviewURL
        broadcastType
        createdAt
        game {
            displayName
            id
            slug
        }
        lengthSeconds
        owner {
            displayName
            id
            login
            profileImageURL(width: 300)
        }
        previewThumbnailURL
        title
    }
}
