query SearchFreeformTags($query: String!, $first: Int) {
    searchFreeformTags(userQuery: $query, first: $first) {
        edges {
            node {
                tagName
            }
        }
    }
}
